
import "lockit_globals.scr"

sub main()
{
    Entry(`obj_7561458345084337339_descr`)
    {
        Text(En, ALL, `Follow the notes left by your father...`);
    }
    Entry(`obj_7561458345084337339_text`)
    {
        Text(En, ALL, `'Twas The Night Before`);
    }
    Entry(`obj_7569258562350256051_compl_desc`)
    {
        Text(En, ALL, `Reconnected the power`);
    }
    Entry(`obj_7569258562350256051_descr`)
    {
        Text(En, ALL, `Reconnect the power`);
    }
    Entry(`obj_7569258562350256051_fail_desc`)
    {
        Text(En, ALL, `-`);
    }
    Entry(`obj_7569258562350256051_text`)
    {
        Text(En, ALL, `Reconnect the Power`);
    }
    Entry(`obj_7569380025532280900_compl_desc`)
    {
        Text(En, ALL, `Found a way into the Tunnels`);
    }
    Entry(`obj_7569380025532280900_descr`)
    {
        Text(En, ALL, `Find a way into the Tunnels`);
    }
    Entry(`obj_7569380025532280900_fail_desc`)
    {
        Text(En, ALL, `-`);
    }
    Entry(`obj_7569380025532280900_text`)
    {
        Text(En, ALL, `Find a way into the Tunnels`);
    }
    Entry(`obj_7569380148656211290_compl_desc`)
    {
        Text(En, ALL, `Found the entrance to the service tunnels beneath the Mall`);
    }
    Entry(`obj_7569380148656211290_descr`)
    {
        Text(En, ALL, `Find the entrance to the service tunnels beneath the Mall`);
    }
    Entry(`obj_7569380148656211290_fail_desc`)
    {
        Text(En, ALL, `-`);
    }
    Entry(`obj_7569380148656211290_text`)
    {
        Text(En, ALL, `Find the entrance to the Mall`);
    }
    Entry(`obj_7569380342958253770_compl_desc`)
    {
        Text(En, ALL, `Find the Fuses`);
    }
    Entry(`obj_7569380342958253770_descr`)
    {
        Text(En, ALL, `Find the Fuses`);
    }
    Entry(`obj_7569380342958253770_fail_desc`)
    {
        Text(En, ALL, `-`);
    }
    Entry(`obj_7569380342958253770_text`)
    {
        Text(En, ALL, `Find the Fuses`);
    }
    Entry(`obj_7569380423401446327_compl_desc`)
    {
        Text(En, ALL, `Ascended to the first fuse box`);
    }
    Entry(`obj_7569380423401446327_descr`)
    {
        Text(En, ALL, `Ascend to the first fuse box`);
    }
    Entry(`obj_7569380423401446327_fail_desc`)
    {
        Text(En, ALL, `-`);
    }
    Entry(`obj_7569380423401446327_text`)
    {
        Text(En, ALL, `Ascend to the first fuse box`);
    }
    Entry(`obj_7569380661422992107_compl_desc`)
    {
        Text(En, ALL, `Added the final fuse`);
    }
    Entry(`obj_7569380661422992107_descr`)
    {
        Text(En, ALL, `Add the final fuse`);
    }
    Entry(`obj_7569380661422992107_fail_desc`)
    {
        Text(En, ALL, `-`);
    }
    Entry(`obj_7569380661422992107_text`)
    {
        Text(En, ALL, `Add the final fuse`);
    }
    Entry(`obj_7569380801914008725_compl_desc`)
    {
        Text(En, ALL, `Returned Home`);
    }
    Entry(`obj_7569380801914008725_descr`)
    {
        Text(En, ALL, `Return Home`);
    }
    Entry(`obj_7569380801914008725_fail_desc`)
    {
        Text(En, ALL, `-`);
    }
    Entry(`obj_7569380801914008725_text`)
    {
        Text(En, ALL, `Return Home`);
    }
    Entry(`obj_7571092618268679440_compl_desc`)
    {
        Text(En, ALL, `Found a way into the Mall Interior`);
    }
    Entry(`obj_7571092618268679440_descr`)
    {
        Text(En, ALL, `Find a way into the Mall Interior`);
    }
    Entry(`obj_7571092618268679440_fail_desc`)
    {
        Text(En, ALL, `-`);
    }
    Entry(`obj_7571092618268679440_text`)
    {
        Text(En, ALL, `Find a way into the Mall Interior`);
    }
    Entry(`obj_7571092810409424612_compl_desc`)
    {
        Text(En, ALL, `Read the Note`);
    }
    Entry(`obj_7571092810409424612_descr`)
    {
        Text(En, ALL, `Read the Note`);
    }
    Entry(`obj_7571092810409424612_fail_desc`)
    {
        Text(En, ALL, `-`);
    }
    Entry(`obj_7571092810409424612_text`)
    {
        Text(En, ALL, `Read the Note`);
    }
    Entry(`obj_7571092878755182904_compl_desc`)
    {
        Text(En, ALL, `Escaped the Mall`);
    }
    Entry(`obj_7571092878755182904_descr`)
    {
        Text(En, ALL, `Escape the Mall`);
    }
    Entry(`obj_7571092878755182904_fail_desc`)
    {
        Text(En, ALL, `-`);
    }
    Entry(`obj_7571092878755182904_text`)
    {
        Text(En, ALL, `Escape the Mall`);
    }
    Entry(`obj_7571092969465631156_compl_desc`)
    {
        Text(En, ALL, `Read the Final Note`);
    }
    Entry(`obj_7571092969465631156_descr`)
    {
        Text(En, ALL, `Read the Final Note`);
    }
    Entry(`obj_7571092969465631156_fail_desc`)
    {
        Text(En, ALL, `-`);
    }
    Entry(`obj_7571092969465631156_text`)
    {
        Text(En, ALL, `Read the Final Note`);
    }
    Entry(`obj_7571093060120271589_compl_desc`)
    {
        Text(En, ALL, `Read the First Note`);
    }
    Entry(`obj_7571093060120271589_descr`)
    {
        Text(En, ALL, `Read the First Note`);
    }
    Entry(`obj_7571093060120271589_fail_desc`)
    {
        Text(En, ALL, `-`);
    }
    Entry(`obj_7571093060120271589_text`)
    {
        Text(En, ALL, `Read the First Note`);
    }
}
