
import "lockit_globals.scr"

sub main()
{
    Entry(`obj_7352800890706720869_descr`)
    {
        Text(En, ALL, `Aiden, having cleared the bandit camp, must return to the survivor settlement to get information about the Vault.`);
    }
    Entry(`obj_7352800890706720869_text`)
    {
        Text(En, ALL, `Secrets`);
    }
}
