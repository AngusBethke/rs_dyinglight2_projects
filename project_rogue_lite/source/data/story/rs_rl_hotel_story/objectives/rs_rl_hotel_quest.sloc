
import "lockit_globals.scr"

sub main()
{
    Entry(`obj_7642373474025521307_descr`)
    {
        Text(En, ALL, `Clear the Hotel and reap the rewards`);
    }
    Entry(`obj_7642373474025521307_text`)
    {
        Text(En, ALL, `The Dead Hotel`);
    }
    Entry(`obj_7653266293111454480_compl_desc`)
    {
        Text(En, ALL, `Found a way into the Hotel`);
    }
    Entry(`obj_7653266293111454480_descr`)
    {
        Text(En, ALL, `Find a way into the Hotel`);
    }
    Entry(`obj_7653266293111454480_text`)
    {
        Text(En, ALL, `Find a way into the Hotel`);
    }
    Entry(`obj_7653266362054194207_compl_desc`)
    {
        Text(En, ALL, `Turned on the Power`);
    }
    Entry(`obj_7653266362054194207_descr`)
    {
        Text(En, ALL, `Turn on the Power`);
    }
    Entry(`obj_7653266362054194207_text`)
    {
        Text(En, ALL, `Turn on the Power`);
    }
    Entry(`obj_7653266464573837569_compl_desc`)
    {
        Text(En, ALL, `Found a way to into the elevator shaft`);
    }
    Entry(`obj_7653266464573837569_descr`)
    {
        Text(En, ALL, `Find a way to into the elevator shaft`);
    }
    Entry(`obj_7653266464573837569_text`)
    {
        Text(En, ALL, `Find a way to into the elevator shaft`);
    }
    Entry(`obj_7653266556545552011_compl_desc`)
    {
        Text(En, ALL, `Got to the Lobby`);
    }
    Entry(`obj_7653266556545552011_descr`)
    {
        Text(En, ALL, `Get to the Lobby`);
    }
    Entry(`obj_7653266556545552011_text`)
    {
        Text(En, ALL, `Get to the Lobby`);
    }
    Entry(`obj_7653266634195795674_compl_desc`)
    {
        Text(En, ALL, `Found a way into the Hotel Rooms`);
    }
    Entry(`obj_7653266634195795674_descr`)
    {
        Text(En, ALL, `Find a way into the Hotel Rooms`);
    }
    Entry(`obj_7653266634195795674_text`)
    {
        Text(En, ALL, `Find a way into the Hotel Rooms`);
    }
    Entry(`obj_7653266727817275943_compl_desc`)
    {
        Text(En, ALL, `Found a way out of the Hotel Rooms`);
    }
    Entry(`obj_7653266727817275943_descr`)
    {
        Text(En, ALL, `Find a way out of the Hotel Rooms`);
    }
    Entry(`obj_7653266727817275943_text`)
    {
        Text(En, ALL, `Find a way out of the Hotel Rooms`);
    }
    Entry(`obj_7653266805502919137_compl_desc`)
    {
        Text(En, ALL, `Got to the 9th Floor`);
    }
    Entry(`obj_7653266805502919137_descr`)
    {
        Text(En, ALL, `Get to the 9th Floor`);
    }
    Entry(`obj_7653266805502919137_text`)
    {
        Text(En, ALL, `Get to the 9th Floor`);
    }
    Entry(`obj_7653266903222006107_compl_desc`)
    {
        Text(En, ALL, `Found a way into the Nest`);
    }
    Entry(`obj_7653266903222006107_descr`)
    {
        Text(En, ALL, `Find a way into the Nest`);
    }
    Entry(`obj_7653266903222006107_text`)
    {
        Text(En, ALL, `Find a way into the Nest`);
    }
    Entry(`obj_7653267092838389103_compl_desc`)
    {
        Text(En, ALL, `Found a way out of the Nest`);
    }
    Entry(`obj_7653267092838389103_descr`)
    {
        Text(En, ALL, `Find a way out of the Nest`);
    }
    Entry(`obj_7653267092838389103_text`)
    {
        Text(En, ALL, `Find a way out of the Nest`);
    }
    Entry(`obj_7653267157986447544_compl_desc`)
    {
        Text(En, ALL, `Got to the 18th Floor`);
    }
    Entry(`obj_7653267157986447544_descr`)
    {
        Text(En, ALL, `Get to the 18th Floor`);
    }
    Entry(`obj_7653267157986447544_text`)
    {
        Text(En, ALL, `Get to the 18th Floor`);
    }
    Entry(`obj_7653267222137881471_compl_desc`)
    {
        Text(En, ALL, `Found a way to the Roof`);
    }
    Entry(`obj_7653267222137881471_descr`)
    {
        Text(En, ALL, `Find a way to the Roof`);
    }
    Entry(`obj_7653267222137881471_text`)
    {
        Text(En, ALL, `Find a way to the Roof`);
    }
    Entry(`obj_7659471787393921951_compl_desc`)
    {
        Text(En, ALL, `Spoke with Spike`);
    }
    Entry(`obj_7659471787393921951_descr`)
    {
        Text(En, ALL, `Speak with Spike`);
    }
    Entry(`obj_7659471787393921951_fail_desc`)
    {
        Text(En, ALL, `Did not speak with Spike`);
    }
    Entry(`obj_7659471787393921951_text`)
    {
        Text(En, ALL, `Speak with Spike Outside`);
    }
    Entry(`obj_7661569904552543354_compl_desc`)
    {
        Text(En, ALL, `Spoke with Spike`);
    }
    Entry(`obj_7661569904552543354_descr`)
    {
        Text(En, ALL, `Speak with Spike`);
    }
    Entry(`obj_7661569904552543354_fail_desc`)
    {
        Text(En, ALL, `Did not speak with Spike`);
    }
    Entry(`obj_7661569904552543354_text`)
    {
        Text(En, ALL, `Speak with Spike in the Lobby`);
    }
    Entry(`obj_7661577617261811968_compl_desc`)
    {
        Text(En, ALL, `Spoke with Spike`);
    }
    Entry(`obj_7661577617261811968_descr`)
    {
        Text(En, ALL, `Speak with Spike`);
    }
    Entry(`obj_7661577617261811968_fail_desc`)
    {
        Text(En, ALL, `Did not speak with Spike`);
    }
    Entry(`obj_7661577617261811968_text`)
    {
        Text(En, ALL, `Speak with Spike on the 9th Floor`);
    }
    Entry(`obj_7661577850786477355_compl_desc`)
    {
        Text(En, ALL, `Spoke with Spike`);
    }
    Entry(`obj_7661577850786477355_descr`)
    {
        Text(En, ALL, `Speak with Spike`);
    }
    Entry(`obj_7661577850786477355_fail_desc`)
    {
        Text(En, ALL, `Did not speak with Spike`);
    }
    Entry(`obj_7661577850786477355_text`)
    {
        Text(En, ALL, `Speak with Spike on the 18th Floor`);
    }
    Entry(`obj_7664677369960456529_compl_desc`)
    {
        Text(En, ALL, `Defeated the Banshee`);
    }
    Entry(`obj_7664677369960456529_descr`)
    {
        Text(En, ALL, `Defeat the Banshee`);
    }
    Entry(`obj_7664677369960456529_fail_desc`)
    {
        Text(En, ALL, `Failed to defeat the Banshee`);
    }
    Entry(`obj_7664677369960456529_text`)
    {
        Text(En, ALL, `Defeat the Banshee`);
    }
    Entry(`obj_7664679189451348134_compl_desc`)
    {
        Text(En, ALL, `Cleared the 2nd Floor Patio`);
    }
    Entry(`obj_7664679189451348134_descr`)
    {
        Text(En, ALL, `Clear the 2nd Floor Patio`);
    }
    Entry(`obj_7664679189451348134_fail_desc`)
    {
        Text(En, ALL, `Failed to clear the 2nd Floor Patio`);
    }
    Entry(`obj_7664679189451348134_text`)
    {
        Text(En, ALL, `Clear the 2nd Floor Patio`);
    }
    Entry(`obj_7664679249623106331_compl_desc`)
    {
        Text(En, ALL, `Cleared the THV Ward`);
    }
    Entry(`obj_7664679249623106331_descr`)
    {
        Text(En, ALL, `Clear the THV Ward`);
    }
    Entry(`obj_7664679249623106331_fail_desc`)
    {
        Text(En, ALL, `Failed to clear the THV Ward`);
    }
    Entry(`obj_7664679249623106331_text`)
    {
        Text(En, ALL, `Clear the THV Ward`);
    }
    Entry(`obj_7664679297393207202_compl_desc`)
    {
        Text(En, ALL, `Cleared the 4th Floor Patio`);
    }
    Entry(`obj_7664679297393207202_descr`)
    {
        Text(En, ALL, `Clear the 4th Floor Patio`);
    }
    Entry(`obj_7664679297393207202_fail_desc`)
    {
        Text(En, ALL, `Failed to clear the 4th Floor Patio`);
    }
    Entry(`obj_7664679297393207202_text`)
    {
        Text(En, ALL, `Clear the 4th Floor Patio`);
    }
    Entry(`obj_7664681763443440234_compl_desc`)
    {
        Text(En, ALL, `Cleared the Hotel Rooms`);
    }
    Entry(`obj_7664681763443440234_descr`)
    {
        Text(En, ALL, `Clear the Hotel Rooms`);
    }
    Entry(`obj_7664681763443440234_fail_desc`)
    {
        Text(En, ALL, `Failed to clear the Hotel Rooms`);
    }
    Entry(`obj_7664681763443440234_text`)
    {
        Text(En, ALL, `Clear the Hotel Rooms`);
    }
    Entry(`obj_7664683321123032391_compl_desc`)
    {
        Text(En, ALL, `Cleared the Parking Lot`);
    }
    Entry(`obj_7664683321123032391_descr`)
    {
        Text(En, ALL, `Clear the Parking Lot`);
    }
    Entry(`obj_7664683321123032391_fail_desc`)
    {
        Text(En, ALL, `Failed to clear the Parking Lot`);
    }
    Entry(`obj_7664683321123032391_text`)
    {
        Text(En, ALL, `Clear the Parking Lot`);
    }
    Entry(`obj_7665008025942179285_compl_desc`)
    {
        Text(En, ALL, `Cleared the first part of the Nest`);
    }
    Entry(`obj_7665008025942179285_descr`)
    {
        Text(En, ALL, `Clear the first part of the Nest`);
    }
    Entry(`obj_7665008025942179285_fail_desc`)
    {
        Text(En, ALL, `Failed to clear the first part of the Nest`);
    }
    Entry(`obj_7665008025942179285_text`)
    {
        Text(En, ALL, `Clear the first part of the Nest`);
    }
    Entry(`obj_7665008442160418991_compl_desc`)
    {
        Text(En, ALL, `Cleared the second part of the Nest`);
    }
    Entry(`obj_7665008442160418991_descr`)
    {
        Text(En, ALL, `Clear the second part of the Nest`);
    }
    Entry(`obj_7665008442160418991_fail_desc`)
    {
        Text(En, ALL, `Failed to clear the second part of the Nest`);
    }
    Entry(`obj_7665008442160418991_text`)
    {
        Text(En, ALL, `Clear the second part of the Nest`);
    }
    Entry(`obj_7665008609953768263_compl_desc`)
    {
        Text(En, ALL, `Cleared the third part of the Nest`);
    }
    Entry(`obj_7665008609953768263_descr`)
    {
        Text(En, ALL, `Clear the third part of the Nest`);
    }
    Entry(`obj_7665008609953768263_fail_desc`)
    {
        Text(En, ALL, `Failed to clear the third part of the Nest`);
    }
    Entry(`obj_7665008609953768263_text`)
    {
        Text(En, ALL, `Clear the third part of the Nest`);
    }
    Entry(`obj_7665008756865116315_compl_desc`)
    {
        Text(En, ALL, `Cleared the fourth part of the Nest`);
    }
    Entry(`obj_7665008756865116315_descr`)
    {
        Text(En, ALL, `Clear the fourth part of the Nest`);
    }
    Entry(`obj_7665008756865116315_fail_desc`)
    {
        Text(En, ALL, `Failed to clear the fourth part of the Nest`);
    }
    Entry(`obj_7665008756865116315_text`)
    {
        Text(En, ALL, `Clear the fourth part of the Nest`);
    }
    Entry(`obj_7665008918205667054_compl_desc`)
    {
        Text(En, ALL, `Cleared the Gym`);
    }
    Entry(`obj_7665008918205667054_descr`)
    {
        Text(En, ALL, `Clear the Gym`);
    }
    Entry(`obj_7665008918205667054_fail_desc`)
    {
        Text(En, ALL, `Failed to clear the Gym`);
    }
    Entry(`obj_7665008918205667054_text`)
    {
        Text(En, ALL, `Clear the Gym`);
    }
    Entry(`obj_7665008997144355879_compl_desc`)
    {
        Text(En, ALL, `Cleared the restaurant`);
    }
    Entry(`obj_7665008997144355879_descr`)
    {
        Text(En, ALL, `Clear the restaurant`);
    }
    Entry(`obj_7665008997144355879_fail_desc`)
    {
        Text(En, ALL, `Failed to clear the restaurant`);
    }
    Entry(`obj_7665008997144355879_text`)
    {
        Text(En, ALL, `Clear the restaurant`);
    }
    Entry(`obj_7665009164230062984_compl_desc`)
    {
        Text(En, ALL, `Cleared the Roof`);
    }
    Entry(`obj_7665009164230062984_descr`)
    {
        Text(En, ALL, `Clear the Roof`);
    }
    Entry(`obj_7665009164230062984_fail_desc`)
    {
        Text(En, ALL, `Failed to clear the Roof`);
    }
    Entry(`obj_7665009164230062984_text`)
    {
        Text(En, ALL, `Clear the Roof`);
    }
    Entry(`obj_7665014080864317026_compl_desc`)
    {
        Text(En, ALL, `Cleared the Rooms`);
    }
    Entry(`obj_7665014080864317026_descr`)
    {
        Text(En, ALL, `Clear the Rooms`);
    }
    Entry(`obj_7665014080864317026_fail_desc`)
    {
        Text(En, ALL, `Failed to clear the Rooms`);
    }
    Entry(`obj_7665014080864317026_text`)
    {
        Text(En, ALL, `Clear the Rooms`);
    }
    Entry(`obj_7665016572053355361_compl_desc`)
    {
        Text(En, ALL, `Found a way into the Rooms`);
    }
    Entry(`obj_7665016572053355361_descr`)
    {
        Text(En, ALL, `Find a way into the Rooms`);
    }
    Entry(`obj_7665016572053355361_fail_desc`)
    {
        Text(En, ALL, `Could not find a way into the Rooms`);
    }
    Entry(`obj_7665016572053355361_text`)
    {
        Text(En, ALL, `Find a way into the Rooms`);
    }
    Entry(`obj_7665016680409989464_compl_desc`)
    {
        Text(En, ALL, ``);
    }
    Entry(`obj_7665016680409989464_descr`)
    {
        Text(En, ALL, ``);
    }
    Entry(`obj_7665016680409989464_fail_desc`)
    {
        Text(En, ALL, ``);
    }
    Entry(`obj_7665016680409989464_text`)
    {
        Text(En, ALL, `New Objective`);
    }
    Entry(`obj_7665390306853855964_compl_desc`)
    {
        Text(En, ALL, `Found the missing scouts`);
    }
    Entry(`obj_7665390306853855964_descr`)
    {
        Text(En, ALL, `Find the missing scouts`);
    }
    Entry(`obj_7665390306853855964_fail_desc`)
    {
        Text(En, ALL, `Could not find the missing scouts`);
    }
    Entry(`obj_7665390306853855964_text`)
    {
        Text(En, ALL, `Find the missing scouts`);
    }
}
